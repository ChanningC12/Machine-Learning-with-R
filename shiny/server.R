# set of instruction that uses the input provided by the user, process them and produces the required output which is further displayed by ui.r script
library(shiny)

shinyServer(
    
    function(input, output) {
        
    }
    )